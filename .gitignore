# Ignorar el entorno virtual
.venv_new/
.venv/
env/
venv/
*.pyc
__pycache__/

# Ignorar archivos temporales y de configuración del IDE
.DS_Store
.vscode/
*.log
*.tmp
*.temp

# Ignorar la carpeta 'asset' que contiene modelos descargados por ChatTTS
asset/

# Ignorar todos los archivos generados dentro de las carpetas,
# PERO MANTENER LAS CARPETAS VACÍAS GRACIAS A .gitkeep
audio/*
!audio/.gitkeep  # Excepción para mantener la carpeta 'audio' vacía pero trackeada

final_video/*
!final_video/.gitkeep # Excepción para mantener la carpeta 'final_video' vacía pero trackeada

subtitles/*
!subtitles/.gitkeep # Excepción para mantener la carpeta 'subtitles' vacía pero trackeada

texts/*
!texts/.gitkeep # Excepción para mantener la carpeta 'texts' vacía pero trackeada
# Si input_text.txt es un archivo de entrada que no se genera y quieres mantenerlo,
# descomenta la siguiente línea:
#!texts/input_text.txt 

videos/*
!videos/.gitkeep # Excepción para mantener la carpeta 'videos' vacía pero trackeada
# Si Minecraft.mp4 es un archivo de entrada que no se genera y quieres mantenerlo,
# descomenta la siguiente línea:
#!videos/Minecraft.mp4

# Ignorar archivos específicos generados en la raíz del proyecto si los hubiera
output_generated_audio.wav
output_video_with_subtitles.mp4
output_generated_subtitles.srt
whisper_result.json

# Archivos de modelos de voz que SÍ deben ser trackeados (no se ignoran)
# voices/speaker_men.pt
# voices/speaker_women.pt

# Archivo principal que SÍ debe ser trackeado (no se ignora)
# main.py

# Archivo de dependencias que SÍ debe ser trackeado (muy recomendado)
# requirements.txt
